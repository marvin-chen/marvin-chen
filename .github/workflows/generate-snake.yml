name: Update Snake Animation

on:
  schedule:
    - cron: "0 0 * * *" # every day at midnight UTC
  workflow_dispatch:

jobs:
  ping-vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Vercel Deployment
        run: curl -s https://github-snake-nnwx7v1ec-marvin-chens-projects.vercel.app/github-contribution-grid-snake.svg?username=marvin-chen
